"""
Django settings for beyond_hunger project.

Generated by 'django-admin startproject' using Django 5.1.7.

For more information on this file, see
https://docs.djangoproject.com/en/5.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.1/ref/settings/
"""

from pathlib import Path
import os
from django.core.management.utils import get_random_secret_key

# Import dj_database_url safely
try:
    import dj_database_url
except ImportError:
    # dj_database_url is not installed, create a dummy function to avoid errors
    def dj_database_url_config(**kwargs):
        return {}
    
    # Create a dummy module to support the import
    class DjDatabaseUrl:
        def config(self, **kwargs):
            print("WARNING: dj_database_url is not installed. Install with pip install dj-database-url")
            return {}
    
    dj_database_url = DjDatabaseUrl()

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Load environment variables from .env file if it exists
try:
    from dotenv import load_dotenv
    load_dotenv(os.path.join(BASE_DIR, '.env'))
except ImportError:
    pass

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.environ.get('DJANGO_SECRET_KEY', 'django-insecure-qwxj%y^h-fvgx&nq!18_dtn-&i4--7!%kdt%*3ov#(qxx@c%4-')

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.environ.get('DJANGO_DEBUG', 'False') == 'True'

# Allow all hosts by default, but override in production
ALLOWED_HOSTS = os.environ.get('DJANGO_ALLOWED_HOSTS', 'localhost,127.0.0.1,beyoundhunger.onrender.com').split(',')

# Check if running on Render
IS_RENDER = os.environ.get('RENDER', '') == 'true'

# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'food_donation',
    'rest_framework',
    'rest_framework.authtoken',
    'whitenoise.runserver_nostatic',  # For serving static files
    'corsheaders',  # For CORS support
    
    # Django AllAuth
    'django.contrib.sites',
    'allauth',
    'allauth.account',
    'allauth.socialaccount',
    'allauth.socialaccount.providers.google',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',  # For serving static files
    'corsheaders.middleware.CorsMiddleware',  # For CORS
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    # Only enable debug middleware in development
    *(['food_donation.middleware.DeploymentDebugMiddleware'] if DEBUG else []),
    # Uncomment if needed for login control
    # 'food_donation.middleware.LoginRequiredMiddleware',
    
    # Django AllAuth
    'allauth.account.middleware.AccountMiddleware',
    'food_donation.middleware.SocialAuthProfileCompletionMiddleware',
    # Visitor tracking middleware
    'food_donation.middleware.VisitorTrackingMiddleware',
]

# Custom authentication backends
AUTHENTICATION_BACKENDS = [
    'django.contrib.auth.backends.ModelBackend',
    'allauth.account.auth_backends.AuthenticationBackend',
]

ROOT_URLCONF = 'beyond_hunger.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'food_donation.context_processors.notifications_processor',
            ],
        },
    },
]

WSGI_APPLICATION = 'beyond_hunger.wsgi.application'


# Database
# https://docs.djangoproject.com/en/5.1/ref/settings/#databases

# If DATABASE_URL is set (as on Render), use that, otherwise use SQLite
DATABASE_URL = os.environ.get('DATABASE_URL')
if DATABASE_URL:
    DATABASES = {
        'default': dj_database_url.config(default=DATABASE_URL, conn_max_age=600)
    }
else:
    # Check if USE_MYSQL environment variable is set
    USE_MYSQL = os.environ.get('USE_MYSQL', 'False').lower() == 'true'

    if USE_MYSQL:
        # MySQL database configuration
        DATABASES = {
            'default': {
                'ENGINE': 'django.db.backends.mysql',
                'NAME': os.environ.get('MYSQL_DATABASE', 'beyondhunger'),
                'USER': os.environ.get('MYSQL_USER', 'root'),
                'PASSWORD': os.environ.get('MYSQL_PASSWORD', ''),
                'HOST': os.environ.get('MYSQL_HOST', 'localhost'),
                'PORT': os.environ.get('MYSQL_PORT', '3306'),
                'OPTIONS': {
                    'charset': 'utf8mb4',
                    'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
                },
            }
        }
    else:
        # Default SQLite database
        DATABASES = {
            'default': {
                'ENGINE': 'django.db.backends.sqlite3',
                'NAME': BASE_DIR / 'db.sqlite3',
            }
        }

# Password validation
# https://docs.djangoproject.com/en/5.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.1/howto/static-files/

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'),
]

# WhiteNoise configuration for static files in production
if not DEBUG:
    STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

# Media files configuration
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# Default primary key field type
# https://docs.djangoproject.com/en/5.1/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Authentication settings
LOGIN_URL = '/login/'
LOGIN_REDIRECT_URL = 'food_donation:home'
LOGOUT_REDIRECT_URL = 'food_donation:home'

# Session settings - increase security
SESSION_COOKIE_AGE = 1209600  # 2 weeks in seconds
SESSION_COOKIE_SECURE = not DEBUG  # Set to True in production with HTTPS
SESSION_SAVE_EVERY_REQUEST = True  # Update session expiry on every request

# Security settings for production
if not DEBUG:
    # HTTPS settings
    SECURE_SSL_REDIRECT = True  # Redirect all requests to HTTPS
    SECURE_HSTS_SECONDS = 31536000  # 1 year
    SECURE_HSTS_INCLUDE_SUBDOMAINS = True
    SECURE_HSTS_PRELOAD = True
    SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
    
    # Cookie security
    CSRF_COOKIE_SECURE = True
    SESSION_COOKIE_SECURE = True

# To prevent immediate login redirects
LOGIN_EXEMPT_URLS = (
    r'^$',  # Home page
    r'^about/$',
    r'^contact/$',
    r'^login/$',
    r'^register/$',
    r'^static/.*',
    r'^media/.*',
)

# CSRF settings
CSRF_COOKIE_AGE = 31449600  # 1 year in seconds
CSRF_USE_SESSIONS = False  # Store CSRF token in cookie, not session
CSRF_COOKIE_HTTPONLY = False  # JavaScript needs to access the cookie

# REST Framework settings
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.TokenAuthentication',
        'rest_framework.authentication.SessionAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ],
}

# CORS settings
CORS_ALLOW_ALL_ORIGINS = DEBUG  # Only allow all origins in development
CORS_ALLOWED_ORIGINS = os.environ.get('CORS_ALLOWED_ORIGINS', 'http://localhost:8000,http://127.0.0.1:8000').split(',')
CORS_ALLOW_CREDENTIALS = True

# CSRF Trusted Origins - Added for PythonAnywhere
CSRF_TRUSTED_ORIGINS = os.environ.get('CSRF_TRUSTED_ORIGINS', 'http://localhost:8000,http://127.0.0.1:8000').split(',')

# Add detailed logging configuration
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format': '[{asctime}] {levelname} {name} {message}',
            'style': '{',
        },
    },
    'handlers': {
        'console': {
            'level': 'DEBUG',
            'class': 'logging.StreamHandler',
            'formatter': 'verbose',
        },
        'file': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': os.path.join(BASE_DIR, 'debug.log'),
            'formatter': 'verbose',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['console', 'file'],
            'level': 'INFO',
            'propagate': True,
        },
        'food_donation': {
            'handlers': ['console', 'file'],
            'level': 'DEBUG',
            'propagate': False,
        },
    },
}

# Handle special debugging environment variables
DISABLE_LOGIN_REDIRECT = os.environ.get('DISABLE_LOGIN_REDIRECT', 'False') == 'True'
DISABLE_CSRF = os.environ.get('DISABLE_CSRF', 'False') == 'True'
USE_DEBUG_MIDDLEWARE = os.environ.get('USE_DEBUG_MIDDLEWARE', 'False') == 'True'

# Disable CSRF temporarily if debugging
if DISABLE_CSRF:
    MIDDLEWARE = [m for m in MIDDLEWARE if 'CsrfViewMiddleware' not in m]

# Enable or disable extra debug middleware
if USE_DEBUG_MIDDLEWARE:
    if 'food_donation.middleware.DeploymentDebugMiddleware' not in MIDDLEWARE:
        MIDDLEWARE.append('food_donation.middleware.DeploymentDebugMiddleware')
else:
    MIDDLEWARE = [m for m in MIDDLEWARE if 'DeploymentDebugMiddleware' not in m]

# Adjust authentication settings for troubleshooting login issues
if DISABLE_LOGIN_REDIRECT:
    LOGIN_URL = '/'
    LOGIN_REDIRECT_URL = '/'
    LOGOUT_REDIRECT_URL = '/'

# Email Configuration
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'beyoundhunger1@gmail.com'
# Use an app password, not your Gmail password
EMAIL_HOST_PASSWORD = ''  # You'll need to set this up with an app password
DEFAULT_FROM_EMAIL = 'Beyond Hunger <beyoundhunger1@gmail.com>'

# AI Chatbot Configuration (OpenAI)
# Note: This API key should be stored as an environment variable in production
# or add to a .env file that is not committed to the repository
OPENAI_API_KEY = os.environ.get('OPENAI_API_KEY', '')  # Set this using environment variables, not in source control

# Django AllAuth settings
SITE_ID = 1

ACCOUNT_EMAIL_VERIFICATION = 'none'
# Replace deprecated settings with new ones
ACCOUNT_SIGNUP_FIELDS = ['email*', 'username*', 'password1*', 'password2*']
ACCOUNT_LOGIN_METHODS = {'username', 'email'}
ACCOUNT_ADAPTER = 'allauth.account.adapter.DefaultAccountAdapter'

# Social account providers
SOCIALACCOUNT_PROVIDERS = {
    'google': {
        'SCOPE': [
            'profile',
            'email',
        ],
        'AUTH_PARAMS': {
            'access_type': 'online',
        }
    }
}

LOGIN_REDIRECT_URL = 'food_donation:home'
ACCOUNT_LOGOUT_REDIRECT_URL = 'food_donation:home'

# PythonAnywhere Compatibility Settings
try:
    from .compatibility import DJANGO_42
except ImportError:
    DJANGO_42 = True

# Force SQLite for PythonAnywhere if requested
if os.environ.get('USE_SQLITE') == 'True':
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.sqlite3',
            'NAME': BASE_DIR / 'db.sqlite3',
        }
    }

# Debug settings for PythonAnywhere deployment
if os.environ.get('PYTHONANYWHERE_DOMAIN'):
    ALLOWED_HOSTS.append(f"{os.environ.get('PYTHONANYWHERE_DOMAIN')}")

# Fix for missing EMAIL_BACKEND
if 'EMAIL_BACKEND' not in locals() and 'EMAIL_BACKEND' not in globals():
    EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
